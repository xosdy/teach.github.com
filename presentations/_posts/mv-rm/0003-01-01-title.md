---
chapter: Move & Remove
layout: slide
title: ''
tags: ['mv-rm']
---

Similarity index

no "move" primitive
score of sameness

	# Move with OS or tool,
	$ mv <FILENAME> <NEWFILENAME>
	# Follow up by staging everything
	$ git add -A .
	# Renames showing
	git status
	# No renames showing?
	git log --stat

	# Renames shown
	git log --stat -M

	# Copies & renames shown
	# (superset of -M)
	git log --stat -C